# Generated by `go generate ./...` (needs clang + bpftool — run in `nix develop`
# or inside the VM). Kernel-specific / build artifacts, so not committed.
/bpf/vmlinux.h
*_bpfel.go
*_bpfeb.go
*_bpfel.o
*_bpfeb.o

# Built binary
/agent
/kestrel-agent
